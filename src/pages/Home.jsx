import React, { useState } from 'react';import { useQuery, useAction, getNotes, deleteNote } from 'wasp/client/operations';import { Link } from 'react-router-dom';const HomePage = () => {const { data: notes, isLoading, error } = useQuery(getNotes);const deleteNoteFn = useAction(deleteNote);if (isLoading) return 'Loading...';if (error) return 'Error: ' + error;const handleDeleteNote = (noteId) => {deleteNoteFn({ noteId });};return (<div className='p-4'>{notes.map((note) => (<div key={note.id} className='bg-white shadow-md p-4 mb-4 rounded-lg flex justify-between items-center'><div><h3 className='text-xl font-bold mb-2'>{note.description}</h3><p className='text-gray-500'>{note.content}</p></div><button onClick={() => handleDeleteNote(note.id)} className='bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded'>Delete</button></div>))}</div>;};export default HomePage;